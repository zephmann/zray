zRay: Monte Carlo Path Tracer

copy zRay directory to Student Home

to compile:
from the zRay directory, type

'g++ main.cpp -fopenmp -O3 -o zRay.out'

copy the code from renderScript to the script editor in maya and run it all
